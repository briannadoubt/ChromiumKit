# ChromiumKit Helper Builder

This internal project builds the bundled `ChromiumKitHelper.app` template that ships inside ChromiumKit's managed host-support resources.

It exists so release preparation and artifact refreshes do not depend on the demo app's project wiring.
